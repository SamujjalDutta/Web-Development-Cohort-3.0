const date = new Date(); // we created a new object of the Date class
console.log(date.toISOString());// we called a function on the object
console.log(date.getFullYear());
console.log(date.getMonth());// july = 6 because it uses 0 based indexing i.e., january = 0th month
